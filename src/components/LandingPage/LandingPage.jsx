import { Grid } from "@mui/material"

export const LandingPage = () =>{
    return(
        <Grid container justifyContent={"center"}>
            <Grid item>Rick and morty - initial</Grid>
        </Grid>
    )
}